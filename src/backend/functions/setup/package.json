{
  "name": "lambda-custom-resource",
  "version": "1.0.0",
  "private": true,
  "description": "Setup the static S3 files",
  "main": "index.js",
  "scripts": {
    "build": "rsync -avz . dist --exclude 'node_modules' --exclude 'test'"
  },
  "dependencies": {
    "aws-sdk": "2.369.0"
  }
}
