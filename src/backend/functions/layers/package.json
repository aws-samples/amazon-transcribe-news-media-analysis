{
  "name": "aws-lambda-layers",
  "version": "1.0.0",
  "private": true,
  "description": "Shared AWS Lambda layers",
  "main": "index.js",
  "scripts": {
    "build": "(cd aws_sdk && npm i --prefix nodejs .) && (cd fp && npm i --prefix nodejs .)"
  }
}
